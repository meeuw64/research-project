W3:
If we were to extend this to arbitrary polytope P and want to guarantee that all the unfoldings are unique, we need more information (namely geometry of the polytope). We would then need a new pipeline to be able to test congruency between two 3D shapes.
In other words: dual graph trees→combinatorial representatives→geometric realization→geometric canonical form
For arbitrary polytopes we need to incorperate the geometry of the shape more explicitly. Weighted graph with distances of the nodes. Not all automorphisms are guarenteed to correspond to symmetries anymore. 

Properties of the 1-skeleton graph of some convex polytope P:
- Finite, simple, connected
- 4-connected (you need to remove at least 4 vertices before the graph becomes disconnected)
- Degree of each vertex is at least 4
- For each vertex v, the neighbors of v inherit the combinatorics of a convex 3-dimensional polytope

W4:
## Meeting:
- #subgroups <-> #unique unfolding relation?
- what are the number of unfoldings in each orbit
- steer towards visualization
- investigate Aut(G_P) ~= Aut(P) for (4D) archimedean solids
- making the research more accessible to other students
- add overlap testing to the pipeline

Supervisor:
1. introduce topics (broadly) -> current challenges -> your solution/contribution
2. related work, how does your work differ, not too technical, bachelor cs should understand
3. background (symmetry groups, graph automorphisms, introduce notation)
4. experiments (1 experiment answers 1 question) (includes "results" section already):
    - motivation
    - setup
    - result
    - interpretation
5. discussion, combination of all experiments combined
6. conclusion ties into the introduction (more in depth)

!!! disclose the use of AI
!!! communicate results instead of process

## Other notes
3D Archimedean solids: symmetry group is isomorphic to dual graph's automorphism group
4D Archimedean solids: ???

Number of spanning trees in complete graph K_n: n^(n-2) , Cayley's formula
Number of graph automorphisms in complete graph K_n: n! , Precisely all permutations of the vertices
Number of edges in complete graph K_n: n choose 2 = n * (n - 1) / 2 , Every vertex is connected to every other vertex

-> runtime is bounded by O(|t(G)| * |Aut(G)| * |E_G|):
    - |t(G)| <= n^(n-2)
    - |Aut(G)| <= n!
    - |E_G| <= n * (n - 1) / 2
    So total runtime upperbound expressed in terms of n = |E_V| (number of vertices):
    O(n^n / n^2 * n! * (n^2 - n) / 2) which is equivalent to O(n^n * n!)

W5:
## Meeting
- Add a parameter to compute |Aut(G_P*)| to investigate symmetries ~= Aut(G_P)
- When does Aut stop corresponding to Symmetries -> do some experiments and report the findings when they stop corresponding
- How in depth should the explanation in the paper be -> add appendix if it goes into too much depth

## Midterm presentation
- Visualize symmetric nets
- RQ -> Provide dataset with all unfoldings
- Provide a counter example where Aut(G_P) != Sym(P)
- Change "shape" to surface of ...
- Every experiment in the paper ties back into one of the research questions, make it explicit